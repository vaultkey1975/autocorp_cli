[pytest]
# Test only AutoCorp CLI's own suite — never the generated projects under
# workspace/ (they have their own test_*.py that need their own deps).
testpaths = tests
pythonpath = .
addopts = -q
norecursedirs = workspace .venv data .git *.egg-info __pycache__
