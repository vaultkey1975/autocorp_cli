"""Universal Fast Pytest Engine.

Repository-agnostic building blocks for running the fastest trustworthy
tests first against any Python project (AutoCorp's own repository or an
external --repo target). Never mutates the target repository or its Git
state; only inspects it.
"""
